{
  "name": "h5ai",
  "version": "0.30.0",
  "scripts": {
    "build": "node ghu release",
    "vercel-build": "npm run build && mkdir -p public && cp -R build/_h5ai/public/* public/",
    "precommit": "npm run -s lint && npm run -s test"
  }
}
